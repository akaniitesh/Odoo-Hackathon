# Required in production. Use a long, randomly generated value.
JWT_SECRET=my-super-secret-development-key

# Required in production. Development uses an ephemeral in-memory MongoDB instance when omitted.
MONGODB_URI="mongodb+srv://nitesh778172_db_user:pHvjuE7X6k5qr9ZH@cluster0.6y7ejbc.mongodb.net/"

PORT=5000
NODE_ENV=development

# Comma-separated browser origins that may call the API.
CLIENT_ORIGINS=http://localhost:5173

# Set to true only when HTTPS terminates before this application in production.
COOKIE_SECURE=false

